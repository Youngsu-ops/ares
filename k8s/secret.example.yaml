# 先复制为 secret.yaml 并填入真实值，且不要提交到 git
# 已将 k8s/secret.yaml 加入 .gitignore
apiVersion: v1
kind: Secret
metadata:
  name: ares-secret
type: Opaque
stringData:
  SESSION_SECRET: "用 openssl rand -hex 32 生成"
  BLOG_ADMIN_PASS: "你的管理员密码"
